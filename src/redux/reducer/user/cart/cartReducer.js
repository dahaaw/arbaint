const initialStateCart = {
    product: 'abaca',
    qty: 5,
    price: 1000,
    total: 5000
}

const cartReducer = (state = initialStateCart, action) => {
    return state;
}

export default cartReducer;